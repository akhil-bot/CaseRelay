# Memory Bank instance — provisioned once, not regenerated by collect_endpoints.sh.
# Sourced by deploy_control_plane.sh alongside fleet_endpoints.env.
export CASERELAY_MEMORY_BANK_ID=8631858420611284992
